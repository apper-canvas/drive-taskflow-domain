import Dashboard from '@/components/pages/Dashboard';

export const routes = [
  {
    id: 'dashboard',
    label: 'Tasks',
    path: '/tasks',
    icon: 'CheckSquare',
    component: Dashboard
  }
];

export default routes;